language: php
php:
  - '5.2'

before_install:
  pear install pear/PHP_CodeSniffer
  - cd $TRAVIS_BUILD_DIR
  - npm install -g npm
  - npm install grunt
  - npm install -g grunt-cli
  - npm install
  - node --version
  - npm --version
  - grunt --version

script: grunt
